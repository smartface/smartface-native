import { AbstractColor } from '../../../ui/color/color';
import { NavigationBarBase, NavigationBarStyle } from './navigationbar';

export default class NavigationBarIOS implements NavigationBarBase {
  color: AbstractColor;
  style: NavigationBarStyle;
}
